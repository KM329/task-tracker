import { TaskHeaderComponent } from './task-header.component';


describe('TaskHeaderComponent', () => {
  let component: TaskHeaderComponent;
 
  beforeEach(() => {
    component = new TaskHeaderComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
